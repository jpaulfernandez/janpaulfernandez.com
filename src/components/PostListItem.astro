---
import { Image } from 'astro:assets';

interface Props {
  title: string;
  type: 'essay' | 'note';
  topics: string[];
  date: string;
  excerpt: string;
  url: string;
  readingTime?: number;
  stage?: 'seedling' | 'budding' | 'evergreen' | 'none';
  cover?: ImageMetadata;
}

const { title, type, topics, date, excerpt, url, readingTime, cover } = Astro.props;
---

<article class="group relative py-8">
  <div class="flex-1 min-w-0 flex flex-col gap-2">
    <p class="m-0 flex flex-wrap items-center gap-x-2 gap-y-1 text-small text-ink-500 tabular-nums">
      <span class="text-wine-500">{type}</span>
      <span aria-hidden="true">·</span>
      <time datetime={date}>
        {new Date(date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}
      </time>
      {readingTime !== undefined && (
        <>
          <span aria-hidden="true">·</span>
          <span>{readingTime} min</span>
        </>
      )}
    </p>

    <h3 class="text-ink-900 leading-tight m-0 text-h3 text-balance">
      <a href={url} class="post-link no-underline text-ink-900 group-hover:text-wine-500 focus:outline-none focus-visible:ring-2 focus-visible:ring-wine-500 rounded-sm">
        {title}
      </a>
    </h3>

    {excerpt && (
      <p class="text-ink-700 text-body leading-relaxed line-clamp-2 m-0 max-w-prose">
        {excerpt}
      </p>
    )}

    {topics.length > 0 && (
      <p class="m-0 flex flex-wrap gap-x-2 text-small text-ink-500">
        {topics.map((topic) => (
          <span>#{topic}</span>
        ))}
      </p>
    )}
  </div>

  {cover && (
    <Image
      src={cover}
      alt=""
      width={120}
      height={80}
      class="hidden sm:block w-20 h-14 shrink-0 object-cover absolute right-0 top-8"
    />
  )}
</article>

<style>
  /* Stretch the title link over the whole row (topics aren't links, so nothing is lost). */
  .post-link::after {
    content: "";
    position: absolute;
    inset: 0;
  }
</style>
