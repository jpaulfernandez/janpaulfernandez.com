---
import '../styles/global.css';

// Import the exact critical font files as URL assets so the preload hrefs
// always match the hashed filenames Astro/Vite emits at build time.
// Phase 8: Courier Prime replaces Archivo Black + Lato; preload 400 + 700.
import courierPrime400Woff2 from '@fontsource/courier-prime/files/courier-prime-latin-400-normal.woff2?url';
import courierPrime700Woff2 from '@fontsource/courier-prime/files/courier-prime-latin-700-normal.woff2?url';

interface Props {
  title: string;
  description: string;
  ogImage?: string;
  type?: 'website' | 'article';
  /** Home page shows the plain title with no site-name suffix. */
  isHome?: boolean;
  bodyClass?: string;
}

const { title, description, ogImage, type = 'website', isHome = false, bodyClass } = Astro.props;

const pageTitle = isHome ? title : `${title} — Paul Fernandez`;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const resolvedOgImage = new URL(ogImage ?? '/og-default.png', Astro.site);

const navItems = [
  { href: '/work-with-me', label: 'work' },
  { href: '/about', label: 'me' },
  { href: '/projects', label: 'projects' },
  { href: '/thoughts', label: 'thoughts' },
  { href: '/now', label: 'now' },
];

const currentPath = Astro.url.pathname;
const isNavActive = (href: string) => {
  if (href === '/thoughts') {
    return currentPath.startsWith('/thoughts') || currentPath.startsWith('/topics');
  }
  return currentPath === href || currentPath.startsWith(`${href}/`);
};

const secondaryLinks = [
  { href: '/gallery', label: 'gallery' },
  { href: '/rss.xml', label: 'rss' },
  { href: 'mailto:jpaul.fernandez18@gmail.com', label: 'email' },
  { href: 'https://www.linkedin.com/in/jpaulfernandez/', label: 'linkedin', external: true },
  { href: 'https://www.instagram.com/goofffball/', label: 'instagram', external: true },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph -->
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:type" content={type} />
    <meta property="og:image" content={resolvedOgImage} />

    <!-- Twitter card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={resolvedOgImage} />

    <!-- Favicon — magenta square (Phase 8). -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- RSS -->
    <link rel="alternate" type="application/rss+xml" title="Paul Fernandez" href="/rss.xml" />

    <!-- Critical font preloads (Courier Prime 400 + 700, self-hosted @fontsource). -->
    <link rel="preload" as="font" type="font/woff2" href={courierPrime400Woff2} crossorigin />
    <link rel="preload" as="font" type="font/woff2" href={courierPrime700Woff2} crossorigin />

    <!-- Google tag (gtag.js) -->
    <script is:inline async src="https://www.googletagmanager.com/gtag/js?id=G-KJX3LCC0SZ"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-KJX3LCC0SZ');
    </script>

    <slot name="head" />
  </head>
  <body class={`min-h-screen flex flex-col ${bodyClass ?? ''}`}>
    <a href="#main" class="skip-link">Skip to content</a>

    <!--
      Phase 8d shell — restored floating sidebar, text right-aligned within it.
        • Mobile (< lg): site header sits inline at the top of the body, right-aligned.
        • lg+: sidebar floats fixed on the left, content right-aligned (so the
          name/nav sits against the article column in the centre, not in the gutter).
        • Main stays centred in the viewport.
    -->
    <header class="px-6 pt-10 pb-8 flex flex-col items-end text-right lg:fixed lg:left-0 lg:top-0 lg:w-52 lg:h-screen lg:overflow-y-auto lg:items-end lg:text-right lg:px-6 lg:py-16">
      <a
        href="/"
        class="site-name block font-bold text-moss-900 no-underline tracking-tight leading-none text-body text-right focus:outline-none focus-visible:ring-2 focus-visible:ring-citrus-500 rounded-sm px-1"
      >
        Paul Fernandez
      </a>

      <span class="site-meta mt-2 block text-moss-500 text-small tracking-wide text-right">
        technologist · philippines
      </span>

      <nav aria-label="Main" class="mt-8 lg:mt-12">
        <ul class="flex flex-wrap justify-end gap-x-6 gap-y-2 list-none m-0 p-0 lg:flex-col lg:items-end lg:gap-y-3">
          {navItems.map((item) => (
            <li>
              <a
                href={item.href}
                aria-current={isNavActive(item.href) ? 'page' : undefined}
                class:list={[
                  'nav-link text-body no-underline text-moss-700 hover:text-moss-900',
                  'focus:outline-none focus-visible:ring-2 focus-visible:ring-citrus-500 rounded-sm px-1',
                ]}
              >
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </nav>

      {/* Secondary links — under the primary nav, right-aligned. */}
      <ul class="mt-10 lg:mt-14 hidden lg:flex flex-col items-end gap-y-2 text-small text-moss-500 list-none m-0 p-0">
        {secondaryLinks.map((link) => (
          <li>
            <a
              href={link.href}
              {...(link.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
              class="text-moss-500 hover:text-moss-900 no-underline transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-citrus-500 rounded-sm px-1"
            >
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </header>

    <!-- Main content — centred in the viewport. -->
    <main id="main" class="flex-1 mx-auto w-full max-w-[64ch] px-6 md:px-12 py-16 md:py-24">
      <slot />
    </main>

    <!-- Footer — centred, same column as <main>. -->
    <footer class="mx-auto w-full max-w-[64ch] px-6 md:px-12 py-10">
      <div class="flex flex-col sm:flex-row sm:items-baseline justify-between gap-3 text-small">
        <p class="m-0 text-moss-500">&copy; {new Date().getFullYear()} Paul Fernandez</p>
        {/* Mobile-only secondary links (< lg), since the lg+ sidebar owns them. */}
        <ul class="flex flex-wrap gap-x-6 gap-y-1 list-none m-0 p-0 lg:hidden">
          {secondaryLinks.map((link) => (
            <li>
              <a
                href={link.href}
                {...(link.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
                class="text-moss-500 hover:text-moss-900 no-underline transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-citrus-500 rounded-sm px-1"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </footer>

    <style>
      /* Magenta ● marks the active page — single accent. The marker is part
       * of the link's inline content, so right-alignment pulls label+marker
       * toward the right edge together. */
      .nav-link[aria-current='page'] {
        color: var(--color-citrus-500);
        font-weight: 700;
      }
      .nav-link[aria-current='page']::before {
        content: '●';
        color: var(--color-citrus-500);
        margin-right: 0.4em;
        font-size: 0.7em;
        vertical-align: 0.25em;
      }
    </style>
  </body>
</html>
