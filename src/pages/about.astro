---
import { getCollection, getEntry, render } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import Timeline from '../components/Timeline.astro';
import JsonLd from '../components/JsonLd.astro';
import { person, profilePage, breadcrumbs } from '../lib/schema';

const aboutEntry = await getEntry('about', 'about');
if (!aboutEntry) {
  throw new Error('About page singleton is missing. Run seed scripts first.');
}
const { Content: AboutIntro } = await render(aboutEntry);

const careerEntries = await getCollection('career');

// Detailed Person info for About page JSON-LD
const detailedPerson = person({
  alumniOf: 'Nueva Ecija University of Science and Technology',
  worksFor: 'Mapúa Malayan Digital College (under MMCL)',
  knowsAbout: [
    'Technology',
    'Digital Transformation',
    'AI Implementation',
    'Economy',
    'Psychology',
  ],
});

const seoData = [
  profilePage(),
  detailedPerson,
  breadcrumbs([
    { name: 'Home', url: '/' },
    { name: 'About', url: '/about' },
  ]),
];
---

<BaseLayout
  title="About"
  description="Learn about Paul Fernandez's technical background, work philosophy, and full career track record."
>
  <JsonLd data={seoData} slot="head" />

  <article>
    <header>
      <h1 class="text-moss-900 m-0">Me</h1>
      <div class="prose mt-6 text-moss-700">
        <AboutIntro />
      </div>
    </header>

    <section class="mt-16 md:mt-20" aria-labelledby="experience-heading">
      <h2 id="experience-heading" class="font-mono uppercase text-xs tracking-widest text-moss-500 m-0 mb-6">
        experience
      </h2>
      <Timeline entries={careerEntries} variant="full" />
    </section>

    <section class="mt-16 md:mt-20 border-t border-moss-200 pt-8" aria-labelledby="contact-heading">
      <h2 id="contact-heading" class="font-mono uppercase text-xs tracking-widest text-moss-500 m-0 mb-4">
        connect
      </h2>
      <p class="text-body text-moss-700 leading-relaxed m-0">
        Available for consultation, talks, and fractional product ownership.
      </p>
      <p class="mt-3 mb-0 text-small">
        <a href="/work-with-me" class="text-moss-900 no-underline hover:text-citrus-500 focus:outline-none focus-visible:ring-2 focus-visible:ring-citrus-500 rounded-sm">work with me →</a>
        <span class="text-moss-500 mx-2" aria-hidden="true">·</span>
        <a
          href="https://www.linkedin.com/in/jpaulfernandez/"
          target="_blank"
          rel="noopener noreferrer"
          class="text-moss-900 no-underline hover:text-citrus-500 focus:outline-none focus-visible:ring-2 focus-visible:ring-citrus-500 rounded-sm"
        >
          linkedin ↗
        </a>
      </p>
    </section>
  </article>
</BaseLayout>
